# Lessons

- If a user screenshot shows UI text that no longer exists in source, treat it as a runtime artifact mismatch first: rebuild + redeploy and verify source/runtime DLL timestamps before further code changes.
- When users need to inspect model space during PLSR Accept/Ignore, keep the review UI modeless and drive apply/cancel via explicit button flags; relying on modal DialogResult blocks pan/zoom and feels like command lockup.
- For UI-driven `Editor.GetEntity` prompts inside ATSBUILD flows, always normalize the command prompt in a `finally` (`WriteMessage` + `PostCommandPrompt`) so stale boundary-pick prompts do not linger on the command bar after returning to the window.
- For UI-initiated editor selection (for example boundary pick from ATSBUILD window), avoid hide/show round-trips; use `Editor.StartUserInteraction(windowHandle)` so AutoCAD command context and prompt lifecycle are released cleanly.
- In ATSBUILD command flows, keep PLSR Accept/Ignore review modal by default; modeless review can leave command-context prompts lingering and invalidate apply state unless command ownership is re-architected.
- In PLSR flows, avoid redundant modal summaries after `PLSR Review`; keep one decision UI and write detailed results to log/file instead of stacking extra popups.
- If users rely on skip diagnostics for reconciliation, do not truncate skipped-item examples; provide the full skipped list (or explicit export) so counts and details align.
- If PLSR missing-label fallback can only create owner/disp text without real source geometry, do not create it (even behind env toggles); skip creation and show a warning dialog with examples instead of producing floating `MText`.
- If users report missing disposition labels with many `Skipped (not closed)`, treat trimmed/open OD polylines as recoverable candidates: attempt bounded endpoint-gap closure + area validation before excluding them.
- When PLSR summary shows high `Missing labels` but low `Actionable results accepted`, keep no-source/no-template rows as explicit skip+warning outcomes; do not create XML/text-only fallback labels.
- If PLSR missing-label rows remain non-actionable due absent quarter-matching source geometry, add a safe same-DISP template fallback (existing label -> new quarter placement) instead of leaving rows as permanent skips.
- When a UI toggle must stay off by default, do not bind its default to persisted `Config` values in shared option catalogs; use explicit unchecked defaults and only override from intentional seed-state recovery.
- In boundary round-trip modal flows, `section_requests_empty` snapshot failures should trigger a bounded reopen (without seed) instead of immediate cancel; otherwise valid surface-impact runs can abort before explicit Build is captured.
- For quarter-dependent workflows, decouple internal quarter-geometry processing from user-visible quarter-definition output: allow processing to run for reliability, then conditionally clean up `L-QSEC`/`L-QUATER` when the `1/4 Definitions` toggle is off.
- In modal UI auto-close recovery paths, never force only one related toggle (for example `Check PLSR`) when multiple dependent options exist; persist and restore the full option set (`Check PLSR` + `Surface Impact`) so recovery behavior matches user intent.
- For UI wording/layout requests, if the user references a recent screenshot, inspect local reference assets first (for example `REFERENCE ONLY`) and align labels/order to that source before asking for additional Figma links.
- Correction-line context cannot rely on plain ATS grid adjacency at township seams; force include shifted cross-range neighbors for seam-edge sections (for example `6 above -> 35 below+range`, and mirror the rule below the seam).
- When users validate one side of a correction seam as fixed, explicitly test/cover the opposite seam direction too; missing symmetric pairs (for example `32 below -> 5 above`) can still drop required RA context.
- Forced correction-context sections can be valid for RA generation but noisy for correction-line/LSD seam inference; keep those scopes decoupled to avoid unintended LSD endpoint behavior.
- For midpoint snapping, prioritize exact regular boundary midpoints first when an LSD endpoint is already on `L-SEC`/`L-USEC`; quarter/component midpoint logic should be fallback, not primary.
- When repeated endpoint heuristics regress nearby correction logic, pivot to an explicit rule-matrix pass keyed by section/quarter instead of layering more fallbacks into the legacy path.
- Treat generic `L-USEC` in user LSD rules as blind-line layer (`L-USEC`) only, not `L-USEC-3018`; if the user says endpoints must never end on 3018, encode that as a hard matrix constraint.
- For Rule 1 (1/4 inner endpoints), derive targets from final adjusted `L-QSEC` components in-model; section-anchor-derived centers/endpoints can be stale after endpoint cleanup and produce exact half-gap misses (for example 5.03 m on west halves).
- When deriving post-adjustment `L-QSEC` component targets, scope candidate components to the owning section envelope; global clip-window clustering can bleed into adjacent sections and pull a 1/4 endpoint onto the neighbor section line.
- In deferred LSD redraw, never erase by "segment intersects requested window" alone; boundary-touching LSDs from adjoining previously-built sections can be deleted. Use quarter ownership (midpoint-in-requested-quarter) for erase scope.
- For horizontal LSD outer endpoints, honor surveyed `L-SEC` as first-choice target; if `L-USEC` is prioritized first, lines can cross the section line into the adjoining side.
- Even with `SEC` priority, midpoint target search must be section-scoped; otherwise global candidate pools can select neighboring-section `L-SEC` midpoints and drive E-W LSDs across the intended boundary.
- Correction seam classification must not rely on north-side evidence only; when north RA is out-of-buffer, use surveyed south-edge horizontal evidence before classifying seam as unsurveyed (`L-USEC-C`).
- For correction-line bugs in 100m-buffer/range-over builds, add explicit seam-input logging (north/south sample contributions, one-sided synthesized seam mode, and xOnly vs seam-band evidence counts) before changing thresholds.
- One-sided synthesized correction seams (missing north/south section samples in 100m-buffer runs) need relaxed surveyed-edge tolerance and x-only fallback checks; strict seam-band edge tolerances can miss valid surveyed south-only evidence and incorrectly classify seams as `L-USEC-C`.
- If ATSBUILD ends mid-run with no exit marker and the last line is `Starting shapefile import.`, treat it as a native importer crash boundary; add step logs around `Importer.Init`/`Importer.Import` and refuse unsafe large-survey imports when location-window setup fails.
- Never call native Map importer on an unvalidated `.shp`: pre-check `.shp/.shx/.dbf` integrity first, and if the direct file is corrupt, resolve the newest valid matching copy before `Importer.Init` to avoid hard process crashes.
- Shape auto-update should copy only configured shape sets (same basename sidecars), not recursive folder contents; recursive selection is allowed only to locate the best valid source set, and destination copy stays flat/specific.
- For quarter-line endpoint snaps near correction seams, strict line-vs-segment intersection is not sufficient; include a bounded apparent-intersection fallback against short segment extensions so 1/4 endpoints can still land on the intended projected hard-boundary intersection.
- For quarter-view 1/4 geometry at blind-line RA crossings, do not rely on endpoint-style anchor intersections; preserve perpendicular crossing by projecting the south correction segment at center-U, then use anchor intersection only as fallback.
- When a user asks to verify an exact landing coordinate, add explicit runtime verification logs with a non-suppressed prefix and do not treat the fix as complete until those logged coordinates are checked against the provided target.
- If quarter south-mid still drifts after a projection fix, verify that correction-south classification is actually active; add a promotion path from regular south boundary to best-fit `L-USEC-C-0` candidate and avoid selecting the absolute south-most correction segment when its road-allowance offset fit is worse.
- For skewed quarter geometry, constant-U projection can miss the intended blind-line corner by a few meters; prioritize apparent intersection against the actual quarter divider (`BottomAnchor -> TopAnchor`) with bounded segment extension, then fall back to projection.
- If the 1/4 south-mid corner still misses by a few meters after pre-clamp fixes, check the post-draw quarter-box vertex snap stage; it can re-drift the computed corner, so protect that shared SW/SE south-mid vertex from generic snap replacement.
- For "extend the 1/4 line to south RA boundary" cases, apply a final south-mid authority pass using the active divider trajectory (`center -> north divider`) intersected with the south boundary segment; this catches residual drift even after pre-clamp fixes and snap guards.
- When users define the target as extending the actual 1/4 line, use the real `L-QSEC` divider segment as the intersection reference; anchor-derived divider proxies can be offset enough to miss the required apparent crossing.
- If divider-extension logs show `found=False` with very large required extension, the south boundary selector likely picked a non-spanning fragment (often east-only). Penalize segments by divider/mid-U coverage gap so the selected south segment actually spans the 1/4 divider crossing.
- For SW quarter SW-corner misses on correction/surveyed cases, resolve the corner from infinite west-boundary x infinite south-boundary intersection (apparent crossing) and avoid re-clamping that resolved point back to fallback south limits.
- If NE/SE quarter corners are built from `frame.EastEdgeU`, sloped east RA boundaries will miss apparent intersections; resolve east boundary from segment geometry and compute `east x south` / `east x north` corner intersections directly.
- When a user reports a "1/4 definition" miss, verify the fix path targets quarter definition objects (`L-QUATER` / `L-QSEC`) and not LSD subdivision-only enforcement paths before accepting the change.
- In LSD outer-endpoint rule-matrix selection, scoring by endpoint move can skip the first valid boundary and cross to a farther one; prefer nearest outward boundary from the inner anchor and preserve endpoints already on preferred boundaries.
- For west-side 1/4 definition points, apply apparent `west x north` intersection logic (not only `west x south`) and protect computed west vertices from generic post-draw corner snapping; otherwise non-correction `NW/W.1/2/SW` points can drift.
- When `VERIFY-QTR-SOUTHMID-DIVEXT-INPUT` shows `found=False` and the selected south segment U-range does not contain the active divider U, treat boundary selection as wrong even if endpoints look plausible; rank south/north candidates with a strong divider-span penalty.
- For blind non-correction 1/4 definitions, divider-span penalty alone can still pick impossible boundary fragments; enforce a hard candidate gate that requires bounded apparent intersection with the active divider (south) and prefer west-linkable north candidates when west-side points are the acceptance criteria.
- In blind non-correction sections, west boundary can be split across north/south halves; do not drive both `S.W.1/4 S.W.` and `N.W.1/4 N.W.` from a single west segment intersection. Add explicit west-south endpoint authority plus west-band hard-corner snap for north corners.
- When a corner is intentionally locked by apparent-intersection/endpoint authority, do not re-clamp its U/V afterward; late clamps can silently reintroduce meter-scale drift.
- For small residual SW-corner drift after apparent west/south intersection (sub-2m scale), add a final high-confidence hard-corner snap (priority 0, tight move cap) rather than retuning global boundary selectors.
- In skewed quarter frames, dot-product world->local conversion can produce meter-scale drift at north/south extremes; use exact basis inversion for authority points (divider endpoints, hard-corner snaps, and west endpoint locks) before writing local coordinates.
- For blind NW corner snap, seeding hard-corner ranking with center-west U can bias to east wrong candidates; seed snap from raw apparent `west x north` intersection instead.
- When a raw apparent `NW` intersection exists, treat it as authority and allow only micro-snap refinement; large blind-snap moves can jump to unrelated north-band corners.
- In skewed quarter frames, any helper that intersects world boundary lines in local space must use exact basis inversion conversion; dot-product local projection inside generic intersection helpers can introduce 20m+ apparent-corner drift even when world-line geometry is correct.
- In blind NW raw-lock logic, a tight negative north-offset gate can reject valid apparent intersections (raw exists but lock does not), causing fallback to unrelated corners; blind sections need a wider negative north-offset acceptance band before snap/fallback.
- Blind SW endpoint authority (`SW-SW-END`) must not apply from midpoint-only west segments; require endpoint proximity to south boundary before override or `sw_sw` can collapse onto `w_half`, creating 3-point quarter definitions.
- Post-draw quarter vertex snap must protect any corner already locked by apparent-intersection authority on east side (`NE.NE`/`SE.SE`) as well as west/south protected corners; otherwise a single hard-corner snap can re-drift a correct quarter-definition corner.
- East-corner protection should not depend only on apparent-lock flags; strict-intersection fallback paths can still yield correct `NE.NE`/`SE.SE` points and must be protected from post-draw snap whenever east boundary + north/south boundary geometry is present.
- For blind sections, selecting east boundary solely by midline proximity to `EastEdgeU` can choose the wrong vertical segment for `NE.NE`; add a north/south-constrained east-segment selector that scores by apparent intersection offsets against both resolved north and south boundaries.
- If an east selector silently returns no candidate, add explicit `found=False` diagnostics before retuning geometry; otherwise repeated fixes can appear as \"no change\" because the new path never engaged.
- For blind non-correction `N.E.1/4` corners, treat the interior hard corner (south-running x west-running `L-SEC/L-USEC-0` node) as final authority; projected east/north intersections can miss that node by large offsets.
- Blind `N.E.1/4` hard-corner snap must reject north/east exterior candidates: enforce positive interior insets (east/north), prefer RA-width inset fit, and block snaps that move farther north/east than raw apparent intersection.
- Stale/overlap cleanup must not erase adjacent 1/4 geometry that only shares a single endpoint with the requested build: require full section ownership for `L-QUATER` erase and skip shortest-wins deletes for single-endpoint touches.
- For `N.E.1/4` on blind sections, cluster-only snapping is not sufficient: solve the corner from the selected east segment intersecting a west-running boundary segment that actually reaches that node (bounded extension), and only use cluster snap as fallback.
- Enforce endpoint authority by section type: non-correction-adjoining sections must resolve `N.E.1/4` at real endpoint nodes (horizontal+vertical endpoint evidence), while endpointless apparent intersections are allowed only on correction-adjoining sections.
- After introducing section-type endpoint authority, prune dead/unsafe fallback paths: non-correction NE branches should not continue into hard-node/cluster fallbacks, or they can reintroduce endpoint misses.
- When post-correction LSD endpoint enforcement runs, never pass combined requested+context quarter infos into the rule-matrix; scope quarter contexts to requested ids only or adjacent prebuilt sections can be mutated by later builds.
- `Check PLSR` must not be gated by `Disposition labels`: always audit existing disposition labels first, reuse same-quarter labels by `dispNum`, and only generate missing labels when label generation is explicitly enabled.
- For user-facing bulk data checks (like PLSR), provide an explicit review gate with per-row `Accept/Ignore` and `Accept All/Ignore All`; only actionable rows should be applied, and non-actionable findings should remain informational.
- For PLSR label matching, never assume the disposition number is the last text line; labels can include trailing status lines like `(Expired)`. Extract disp numbers by prefix+digits from all lines (bottom-up) to avoid false missing-label reports.
- AutoCAD `MText.Contents` may include inline formatting controls (for example `\A1;`); always strip control codes before owner/disp parsing and normalization or PLSR checks will show false mismatches/missing labels.
- In PLSR review UI, distinguish quarter-level findings from disposition-level findings explicitly (for example `Disp=N/A` on `Missing quarter in XML`) so blank disp cells are not mistaken for parsing bugs.
- PLSR review should never display raw `MText.Contents`; flatten sanitized lines for UI output, because raw content carries control groups (`\P`, `\pxqc;`, etc.) that users interpret as parsing failures.
- For PLSR expiry checks, do not create `Add (Expired)` actions when label text already contains an expired marker; detection must be normalized/sanitized (not literal raw `(Expired)` string matching only).
- When copy-to-`build` fails (locked DLL), users may unknowingly run stale plugin code; always verify `bin` vs `build` timestamps/sizes and sync artifacts before asking for retest.
- In review grids, never gate `Accept/Ignore` editing by actionability at cell-edit time; users expect dropdowns to work for all rows. Keep actionability enforcement in apply logic, and force commit of in-cell edits before closing dialogs.
- `Check PLSR` runs must be able to generate missing labels independently of the `Disposition labels` toggle: scan existing OD disposition polylines in requested scope first, then import shapefiles only as fallback when none are present.
- If A-DIM is the required standard, remove style toggles from UI and force aligned-dimension placement in code paths; keeping optional leader toggles invites mixed output styles.
- For PLSR workflows, missing-label generation must be review-driven: do not pre-place all labels when `Check PLSR` is on; surface `Missing label` as actionable and create only accepted rows.
- In mixed-action PLSR apply loops, never gate all actionable rows on `issue.Label != null`; `CreateMissingLabel` actions require `Disposition+Quarter` and must be routed by `ChangeType` so accepted missing-label rows are actually created.
- For `Check PLSR` performance, never force fallback disposition shapefile import before proving missing labels exist; precheck XML vs existing labels first, then import only when missing-label creation is actually needed.
- For `/debug-config` performance, high-volume trace logging must be buffered and selectively suppressed by default; keep deep traces (`TRACE-LSD-*`, per-segment relayer logs, wellsite debug spam) opt-in via env toggles.
- When adding pre-build UI actions that depend on derived geometry (for example quarter definitions), do not assume helper layers (like `L-QUATER`) already exist in drawing state; derive from authoritative section-index inputs in-memory and avoid leaving temporary helper entities visible.
- For PLSR runtime paths, guard AutoCAD DB reads/writes per-entity/per-issue and keep a top-level stage-aware catch in `RunPlsrCheck(...)`; uncaught `Autodesk.AutoCAD.Runtime.Exception` in scan/apply can terminate ATSBUILD like a fatal.
- Boundary-import UI actions should remove placeholder blank grid rows before appending imported entries; leaving a blank first row is usually harmless but creates carry-down ambiguity and hides real failure patterns.
- For native-crash-prone paths (PLSR precheck + shapefile importer), flush boundary log lines immediately (`PLSR precheck`, `Starting shapefile import`, `Importer.Init/Import begin`) so crash location survives abrupt termination.
- For boundary-expanded PLSR runs, any candidate-level geometry test (`IsPointInsidePolyline`, overlap probes, `GeometricExtents`) must be wrapped and skipped per candidate; never allow one malformed polyline to take down the full check.
- Exception code `0xE0434352` indicates a managed exception escaped to host; always add top-level stage-aware catch/logging in command handlers and per-event UI try/catch in WPF/WinForms build submit paths to prevent hard host termination.
- WPF `Window.DialogResult` can only be set for modal dialogs (`ShowDialog`); shared window handlers used by debug tools must use a guarded close helper that falls back to `Close()` for modeless launches.
- After making modal-safe close changes, do not keep ATSBUILD gated on `ShowDialog()==true`; accept a populated UI result payload (`window.Result != null`) as the execution signal or valid builds will be logged as `ui_cancelled`.
- Do not treat generic UI auto-close (`DialogResult != true` with no build intent flags) as permission to run ATSBUILD; fallback recovery must require explicit build intent (`BuildRequested` or `BuildAttempted`) unless there is an explicit opt-in auto-run mode.
- In WPF modal flows, `Hide()` used for in-drawing boundary selection can produce a false dialog return before Build; handle this as a boundary round-trip resume case (reopen dialog with seeded state), not as cancel and not as implicit auto-run.
- If UI closes with `ExplicitCancel=False` and no build-intent flags, prefer reopen-with-seeded-state (explicit Build still required) over immediate cancel; cap reopen retries and gate snapshot-run fallback to non-aborted build traces to avoid both self-cancel and unintended autorun.
- For modal/no-result UI recovery, always instrument section payload flow (`grid rows -> parsed requests -> snapshot -> final input`) before changing fallback logic again; otherwise duplicate-window artifacts can hide whether the real fault is lifecycle or dropped section state.
- Once behavior is confirmed stable by the user, remove temporary high-volume diagnostics and keep only durable guardrails; leave enough logs for state transitions, not full payload dumps.
- Persisted runtime options used for backend recovery (for example PLSR fallback options) should not also be applied as constructor-time UI defaults; boundary/no-seed reopen flows can unintentionally turn user-facing toggles ON.
- In PLSR fallback mode, never feed scanned existing disposition entity IDs into cleanup erase paths; keep imported IDs separate and only erase imported disposition linework when linework output is off.
- In PLSR-only runs (`Check PLSR` on, disposition linework/labels off), do not gate fallback disposition import on `existingDispositionCount == 0`; partial existing coverage can still leave many missing labels without source geometry, so run missing-label precheck and allow supplemental import when gaps exist.
- If a PLSR supplemental import path reaches `Importer.Import begin.` and AutoCAD hard-terminates, default back to non-import behavior for partial existing-coverage runs and require explicit env opt-in (`ATSBUILD_PLSR_SUPPLEMENT_IMPORT=1`) before re-entering the native importer path.
- When `/debug-config` still creates far fewer labels than missing count, check whether `Actionable results accepted` is low due strict disp-number normalization; normalize DISP_NUM canonically (strip non-alnum, normalize prefix+numeric suffix, trim numeric leading zeros) so existing/source matching and per-quarter dedupe use the same canonical key.
- If ATS Fabric (sections) is enabled, keep 1/4 linework visible even when the UI `1/4 Definitions` toggle is off; cleanup should only erase quarter view/helper lines when ATS Fabric, 1/4 Definitions, and env quarter-view override are all off.
- When ATS Fabric must preserve quarter helper behavior but `1/4 Definitions` is off, split cleanup gates by layer role: `L-QUATER` should follow the explicit 1/4 display toggle/env only, while ATS-specific helper-line retention can remain independent.
- For multi-toggle geometry visibility behaviors, centralize decision logic in one pure policy helper and consume it from both planning and cleanup paths to prevent matrix drift bugs.
- For complex cleanup behavior, separate decision computation from AutoCAD side effects with a pure cleanup plan object; this keeps toggle matrices testable and prevents regressions when new visibility rules are added.
- If ATSBUILD logs stop at `Importer.Import begin.` with no exit marker, treat it as a native Map importer hard-crash boundary and add pre-import risk guards (size/unsafe conditions) with explicit env override instead of invoking native import unconditionally.
- For very large disposition shapefiles (`DAB_APPL`-scale), do not call native `Importer.Import()` on full source files; build a temporary spatially filtered SHP/SHX/DBF subset first and import that subset to avoid AutoCAD host termination.
- If raw spatial subset filtering returns zero matches on large shapefiles, do not skip import outright; fall back to chunked safe import because CRS/projection differences can make raw coordinate filtering miss valid in-scope records.
- For ATS-fabric-off large imports, treat per-chunk MPOLYGON volume as a crash risk; keep default chunk sizes conservative (smaller) and only scale up via env override after stability is proven.
- Do not rely on default `scopeBufferMeters` for ATS disposition imports; pass the intended buffer explicitly (`100m`) so runtime scope and log output match user expectations.
- If large-file spatial subset keeps returning zero matches while native import still finds features, verify SHP CRS first; when source is geographic and drawing is UTM, transform section extents to source coordinates (zone-aware) before record-bounds filtering, otherwise performance collapses into full chunk fallback.
- If a new import acceleration path is unstable in one execution mode (for example ATS Fabric OFF), gate the optimization by mode and keep the proven stable fallback for that mode instead of forcing one path for all builds.
- If logs still stop at `Importer.Import begin.` while importing a generated spatial-subset shapefile, treat subset-file import itself as unstable and default large-file runs to chunked source import; keep subset-file import behind explicit env opt-in only.
- When a user asks for validation linework only as a check, keep it behind an explicit UI toggle and avoid forcing always-on drawing behavior.
- For folder-level photo QA workflows, expose them as explicit palette actions with simple picker prompts (buffer + folder) rather than overloading the main build command flow.
- For zone-specific buffer modes (for example `PROPOSED / 100m`), prompt for each zone's boundary and block explicitly, and use deterministic zone precedence to prevent duplicate inserts in overlapping areas.
- When WLS location rows must respect ATS road-allowance quarter geometry, resolve findings against existing `L-QUATER`/`L-QUARTER` polygons first and only fall back to synthetic section-index quarter splits if no layer polygon contains the point.
- WLS table location quarter tokens should be rendered as dotted legal abbreviations (`N.W.`, `N.E.`, `S.W.`, `S.E.`), not compact `NW/NE/SW/SE`.
- When WLS users request legal-location detail beyond quarter direction, use ATS-compatible LSD numbering (`4x4` serpentine per section) and output `LSD-SEC-TWP-RGE-WMER` rather than quarter tokens.
- For WLS `PROPOSED / 100m` table outputs, enforce explicit group ordering (`PROPOSED` then `100m`) and render a visible blank separator row between groups to match review workflow expectations.
- Photo caption styling requests may target only part of the string (`PHOTO #` vs number); use MText inline color formatting so label text color can differ from numeric token color without affecting the finding-description line.
- For WLS quarter-boundary resolution, treat ATS `L-QUATER` as authoritative quarter geometry (road-allowance-aware) and only fall back to `L-QUARTER` when `L-QUATER` is absent.
- If quarter validation linework is meant to verify ATS quarter usage, do not mix in synthetic section-index quarter polygons; validation output should reflect matched quarter-layer polygons only.
- When users ask for photo caption style changes, apply both text transform and placement offset explicitly (`ALL CAPS`, color, and exact Y shift) rather than partial formatting tweaks.
- For WLS grouped table spacer rows (for example between `PROPOSED` and `100m`), treat them as visual separators: side/vertical borders off, top+bottom borders on, and explicit row height so print layout is predictable.
- For WLS ATS quarter matching, do not hardcode one quarter layer source before findings are known; load all candidate quarter layers and select the source that maximizes containment on active findings, with ATS `L-QUATER` as tie-break preference.
- If WLS L-QUARTER validation linework is enabled, do not require strict containment-only matches to draw diagnostics; add nearest-quarter fallback drawing and explicit source-selection fallback so users always get visible validation geometry when quarter layers exist.
- For WLS ATS quarter-boundary fidelity, if the user requests ATS-only behavior, enforce `L-QUATER`-only source loading (no `L-QUARTER` fallback) so road-allowance quarter logic always comes from ATS-built quarter view geometry.
- If the user says validation must use/draw `L-QUARTER` (not `L-QUATER`), make quarter-source loading include and prioritize `L-QUARTER`; do not hard-lock to ATS `L-QUATER` only.
- Keep a single canonical quarter layer name in WLS (`L-QUARTER`); remove legacy `L-QUATER` aliases from matching logic to avoid ambiguous source selection and user confusion.
- For WLS L-QUARTER validation drawing, do not depend solely on existing quarter layer polylines; when sources are absent, populate validation polygons from `AtsQuarterLocationResolver` quarter vertices so ATS road-allowance logic still draws for user verification.
- When WLS must match ATS quarter-definition behavior (road allowances, 20.12-offset workflow), use ATS section-build internals to generate quarter definitions and immediately run ATS cleanup to strip temporary section linework, rather than relying on simplified in-house frame splitting.
- For quarter/LSD boundary resolution regressions, do not let `L-USEC-0` silently win when 20.12-class candidates exist on requested west/south sides; add an explicit preferred-layer promotion pass (with zero-offset fallback) after initial selection.
- For LSD endpoint enforcement, do not assume `onThirty` endpoints can always resolve within strict midpoint/axis thresholds; keep a widened 30.18-near tolerance and a relaxed hard-boundary escape fallback so `L-SECTION-LSD` endpoints cannot remain on `L-USEC-3018`.
- In LSD endpoint enforcement boundary collection, do not require boundary polylines to be collinear/open-only; collect per-segment geometry (including closed polylines) for `L-USEC*`/`L-SEC*` layer scans so 30.18 targets are never missed.
- When adding LSD endpoint diagnostics, instrument both execution paths (TryEnforceLsdLineEndpointsByRuleMatrix and fallback EnforceLsdLineEndpointsOnHardSectionBoundaries); the rule-matrix path can short-circuit and otherwise hide all new traces.
- If LSD rule-matrix logs show repeated outer source=fallback-anchor moved=False and outerAdjusted=0, check boundary midpoint scope gating first; sectionScopePad must accommodate RA-offset boundaries (~20-30m) or TWENTY/ZERO targets are skipped.
- In LSD rule-matrix, do not short-circuit horizontal outer endpoints directly to quarter anchors; route them through preferred boundary midpoint selection (TWENTY/ZERO/SEC) first, with anchor only as fallback, or west/east endpoint fixes will never engage.
- In LSD rule-matrix midpoint targeting, do not always preserve current endpoint on first-priority BLIND segments; parallel pre/post-adjust blind rows can coexist. Require strong hard-link evidence (both endpoints near SEC/0/20/CORRZERO) before freezing BLIND endpoints, otherwise continue scoring.
- In LSD midpoint selector, never return immediately on 'already on primary boundary' checks; mark as preservable and run midpoint scoring first, then preserve only if no valid midpoint candidate exists. Immediate preserve can lock onto stale pre-adjust ZERO/BLIND rows.
- For WLS quarter-location behavior, distinguish clearly between the in-DLL section-index resolver (simple frame split) and the ATS reflection fallback that runs `DrawSectionsFromRequests`; full road-allowance quarter generation requires `AtsBackgroundBuilder.dll` to be loadable.
- For non-correction `N.E. N.E.` quarter corners, never fall back blindly to the highest east-boundary endpoint; require north/horizontal endpoint-node evidence (or near-north-boundary touch), otherwise keep the prior NE candidate to avoid crossing RA.
- For quarter-definition ownership baselines, target full `RoadAllowanceUsecWidthMeters` (20.12-class) for west/south/east side selection; using `RoadAllowanceUsecWidthMeters - RoadAllowanceSecWidthMeters` (10.05-class) under-allocates quarter ownership and can pin the east side on the east edge instead of inward (west).
- In mixed surveyed/unsurveyed quarter boundaries, do not allow west-side preferred-layer promotion or fallback ladders to downgrade from 20.12 ownership to 10.05/0 when south is surveyed; keep downgrade paths only for blind-south cases.
- Apply the same ownership rule to south: for surveyed south boundaries, do not run fallback ladders that can downgrade full 20.12 ownership to SEC/near-edge candidates.
- For quarter-definition ownership, use the full SEC-width inset (`RoadAllowanceSecWidthMeters` ~= 20.11), not `RoadAllowanceUsecWidthMeters` (~30.16); switching to USEC-width over-shifts south/west/east by about 10.05m.
- Quarter ownership inset is side-dependent in mixed surveyed/unsurveyed contexts: south/east can be SEC-width while west may still require USEC-width; avoid applying one global inset constant to all sides.
- For west/south quarter ownership, use layer intent to choose class per side: `L-USEC-0` indicates USEC-width ownership, while `L-SEC`/`L-SEC-2012` indicate SEC-width ownership; do not resolve these sides from broad mixed-layer pools first.
- When users report "old ATS behavior" after a pull, always verify `main` is fast-forwarded to `origin/main`, rebuild both `WildlifeSweeps` and `AtsBackgroundBuilder`, and log the resolved ATS assembly version/path at runtime before further geometry changes.
- When AutoCAD is unavailable/locked, prioritize ATS-only refactors that are behavior-preserving and provable by local build + decision tests; defer any refactor that needs in-drawing validation until user test windows reopen.
- In PLSR flows, never use canonical normalized DISP keys (which may trim leading zeros) as user-facing display/output values; keep a separate display DISP parsed from XML/label text and use canonical keys only for matching/dedupe.
- When users convert a pre-build maintenance toggle into mandatory behavior, remove/ignore the UI toggle and enforce the behavior at input-capture time, then surface explicit confirm/skip and unavailable-source warnings so updates are never silent.
- For network-shared shapefile update checks, never run recursive file scans once per requested base name; resolve anchors via a single recursive *.shp pass and then map requested base names from that index to avoid pre-prompt UI stalls.
- 2026-03-04: Do not front-load user prompts for optional data refresh in PLSR-only flows. First run the runtime precheck/import-need decision, then prompt only if that branch is actually taken.
- 2026-03-04: Shape refresh comparisons should ignore destination-only generated sidecars and treat updates as source-missing/size-change/source-newer to avoid repeated false update prompts.
- 2026-03-04: In PLSR owner reconciliation, enforce XML owner truth at apply-routing time (force-accept UpdateOwner issues) while leaving non-owner actions user-reviewed; this preserves deterministic owner correctness without over-applying other fixes.
- 2026-03-04: In PLSR review, compare OD VER_DATE (yyyyMMdd) to XML <VersionDate> (ISO date-time) by normalized date-only values, and surface a dedicated Ver. Date. status (MATCH / NON-MATCH / N/A) instead of burying it in free-text details.
- 2026-03-04: PLSR version-date checks should emit standalone rows only for NON-MATCH; N/A should be shown only on rows already triggered by other checks (owner/expiry/missing/not-in-PLSR).
- 2026-03-04: For PLSR version-date compare, do not blend XML date sources; use only the explicitly agreed XML field (ActivityDate) to avoid false mismatch/N/A interpretations.
- 2026-03-04: In PLSR version-date mismatch generation, never fall back to first DISP candidate across quarters; only compare against a quarter-resolved source to avoid false NON-MATCH rows.
- 2026-03-04: For PLSR VER_DATE comparison, treat XML VersionDate and ActivityDate as alternate valid targets; report NON-MATCH only when OD date matches neither, and display VersionDate as the primary expected date.
- 2026-03-04: In this PLSR XML, VersionDate is often nested under activity children (e.g., Plans/Plan/VersionDate); direct activity.Element(ns+"VersionDate") reads null and falsely forces ActivityDate fallback.
- 2026-03-05: PLSR date compare must consider both OD fields (`VER_DATE`, `EFFDATE`) against both XML date sources (`VersionDate`, `ActivityDate`); otherwise valid records can be falsely flagged as NON-MATCH.
- 2026-03-05: When the user explicitly scopes a date compare to specific fields (for example XML `VersionDate` and OD `VER_DATE`), remove all alternate fallback sources from both matching logic and mismatch messaging; if either required field is missing, return `N/A` and do not emit a standalone mismatch row.
- 2026-03-05: For mixed `PURPCD` values like `WELLSITE AND ACCESS ROAD`, do not hardcode purpose labels; resolve both variants from purpose lookup and emit separate reuse-keyed label variants so road (`A/R`) and wellsite surface (`LSD + 8-2 (Surface)`) can both be placed when geometry contains both signatures.
- 2026-03-05: For mixed-purpose split labels, layer mapping must be variant-specific: resolve each variant from purpose lookup (value+extra) and apply its own `C/F-<suffix>-T` text layer; reusing the raw mixed PURPCD entry can put `A/R` labels on the wrong layer and hide expected output behavior.
- 2026-03-05: For mixed `WELLSITE AND ACCESS ROAD` records, do not relayer source geometry from the raw mixed PURPCD lookup row; choose line layer from the emitted wellsite variant (`WS`) and choose wellsite text anchor from the widest sampled center (`MaxCenter`) so surface labels land in the pad, not the road corridor.
- 2026-03-05: For mixed road+pad dispositions, centerline/median-based anchors can still park surface text near the corridor; choose the label anchor by maximizing interior boundary clearance inside quarter+disposition overlap so surface labels land in the pad body.
- 2026-03-05: When both `Disposition labels` and `Check PLSR` are enabled, keep shape-based label placement ahead of PLSR audit; gating placement off when PLSR is on makes the check audit an empty label set and falsely flag every disposition as missing.
- 2026-03-05: When PLSR XML is present and a disposition label is created for the first time, use the PLSR owner before label placement; otherwise the build can create its own false owner-mismatch issues for the subsequent PLSR audit.
- 2026-03-05: When PLSR XML is authoritative for first-time label creation, carry both owner and expired state into the label source before the review scan; otherwise the same run can fabricate fresh-label `Expired in PLSR` issues.
- 2026-03-05: Standalone PLSR version-date mismatches should aggregate repeated quarter rows by DISP into one `MULTIPLE` review row, with the quarter list kept in detail instead of spamming one row per quarter.
- 2026-03-05: For large spatially filtered disposition imports, do not automatically re-chunk the filtered subset when it already fits within one safe import file; importing that scoped subset directly is simpler and avoids reintroducing native Map importer instability on an unnecessary second generated shapefile.
- 2026-03-06: For PLSR missing-label detection, do not decide quarter membership from leader text alone; if an `MLeader`'s leader vertices/target land inside the quarter, count the label as present even when the text sits outside the 1/4.
- 2026-03-06: For width-required disposition labels, do not rely on upstream branches alone to mark `Variable Width` review cases; enforce ACI 3 (green) from the final label text so any forced `Variable Width` output is visibly review-required.
- 2026-03-06: For PLSR missing-label detection on width labels drawn as aligned dimensions, do not rely on `DimensionText` position alone; use the dimension's extension-line span/midpoint so slightly outside text does not fabricate missing-label rows.
- 2026-03-06: In PLSR label scanning, do not hard-drop entities only because their layer misses the narrow `C/F-...-T` pattern; if the entity text already parses as a valid disposition label, count it and log the nonstandard layer instead of fabricating a missing-label row.
- 2026-03-06: When a visible disposition text already exists in the target quarter but only survives scan as a nonstandard-layer DISP candidate, suppress the `Missing label` review row instead of offering `Create missing label`.
- 2026-03-06: Any `Autodesk.Gis.Map.ObjectData.Table` fetched from `ODTables[tableName]` must be disposed immediately; leaving Map OD table wrappers for finalization can crash `acad.exe` in `Table.DeleteUnmanagedObject()` with an access violation.
- 2026-03-06: For PLSR label-existence checks, do not rely on a single text insertion point; include sampled text-extents points, leader vertices, and other visible label geometry so any in-quarter portion of the label can satisfy the quarter match.
- 2026-03-06: For PLSR label-existence checks, sampled points alone can still miss visible labels; if the rendered label bounds touch the quarter, count the label as present.
- 2026-03-06: When corridor labels still bunch after basic overlap rejection, the cheapest low-risk improvement is to search farther for leader labels and inflate the collision gap before considering a placement successful.
- 2026-03-06: In PLSR label scanning, do not assign a label to the first quarter that touches it; when a label touches multiple quarters, choose the best quarter from in-quarter point evidence and use bounds overlap only as a weak fallback.
- 2026-03-06: For PLSR label existence, do not collapse a scanned label to one quarter only; let it satisfy every quarter it visibly touches, and reserve the primary-quarter choice for `Not in PLSR` / tie-break reporting only.
- 2026-03-07: For width labels drawn as aligned dimensions, do not reuse the generic spiral text search centered in the corridor; use a dimension-specific candidate search near corridor edges/whitespace and keep TextPosition independent from the dim line so the measurement anchor stays on the corridor while the text can move outside it.
- 2026-03-07: For aligned width labels, center-point offset is not enough; candidate lanes and final dimension creation must enforce clearance using the full estimated text block height against the measured corridor line.
- 2026-03-07: For aligned width labels on bent corridors, never reuse the text-search/leader-avoidance target as the measurement anchor; preserve a dedicated in-corridor measurement target so the A-DIM span stays on the intended cross-section.
- 2026-03-07: For bent corridor width labels, a single global median width sample is not a safe measurement anchor; resolve the A-DIM cross-section from local samples near the quarter/intersection target so the span stays on the correct leg.
- 2026-03-07: For bent corridor width labels, local target resolution must explicitly penalize bend-adjacent cross-sections; proximity-only scoring will still choose transition nodes when the overlap target lies near a bend.
- 2026-03-07: When a user reports a visual behavior "didn't fix it at all," verify the deployed AutoCAD plugin DLL/PDB hashes against the freshly built artifacts before changing geometry logic again; stale deploys can make correct code look ineffective.
- 2026-03-07: When a width A-DIM already measures the correct corridor cross-section, do not relocate the measured span to follow text-placement heuristics; preserve the original span anchor and solve only text placement or fallback style.
- 2026-03-07: For moved aligned-dimension text, keep both the text projection and the dimension-line hinge projection inside the measured span; if only the text moves while the hinge stays at midpoint or drifts past the endpoints, AutoCAD can render missing or misleading leader/jog lines.

- 2026-03-07: When width A-DIM visuals regress after placement, check post-placement text-update paths before blaming cleanup; in this codebase cleanup only erases helper geometry, but PLSR can reopen dimensions for write and trigger AutoCAD recompute unless text position/span constraints are re-applied.

- 2026-03-08: Do not run a generic aligned-dimension re-clamp after creation or every text mutation; if a width A-DIM is already placed correctly, preserve its captured TextPosition/DimLinePoint across PLSR text rewrites instead of recomputing geometry.

- 2026-03-08: If the latest geometry heuristics make placement broadly worse, stop layering more fixes and roll back to the last narrower good state; preserve the proven overlap improvements, but remove experimental A-DIM line-preservation logic quickly.
- 2026-03-08: When a user points to a concrete reference/example file for a new input format, inspect that file immediately and use it as the source-of-truth for parser assumptions before wiring the feature.
- 2026-03-08: Do not use a runtime behavior as proof of a new deploy if the new code intentionally preserves that behavior; verify loaded-command differences or file hashes instead of choosing a signal that is unchanged by design.
- 2026-03-09: When WLS offers parallel buffer modes (for example `PROPOSED / 100m` and `PROPOSED / 100m / OUTSIDE`), keep the prompt/classification path shared; do not let one mode silently fall back to a single 100m boundary or hardcoded outside block, or selection behavior will diverge from the user’s mental model.
- 2026-03-09: For first-pass AutoCAD plotting, do not assume forced device/media/style calls will be accepted uniformly at runtime; initialize from the current layout config, apply preferred PDF/color settings defensively, and emit step-specific diagnostics so eInvalidInput can be localized quickly.
- 2026-03-09: If managed AutoCAD plotting keeps failing at `PlotInfoValidator` media matching while the same page setup works in the UI, stop iterating on validator/media probes and switch to a native `-PLOT` flow that reuses the current model page setup and injects only the ATS window/scale/output path.
- 2026-03-09: In `ATSPLOT_AUTO` native `-PLOT` device reuse, do not treat any `PDF` device as safe; explicitly reject `Adobe PDF` and only reuse AutoCAD `DWG To PDF` devices, otherwise PostScript font popups can block unattended plotting.
- 2026-03-09: For `DWG To PDF.pc3`, native `-PLOT` prompt trees do not include a write-to-file Yes/No step before file name; script tokens must map exactly to file-name -> save-page-setup -> proceed prompts, or AutoCAD drops into interactive Yes/No loops.
- 2026-03-09: In unattended ATSPLOT flows, warning-only on oversized 1:5000 windows is not enough; automatically fall back to `Fit` scale when paper cannot contain the saved window so the output still captures the full build area.
- 2026-03-10: For CSV fields like meridian values (`5 -5`) that Excel may auto-convert into dates on open, emit them in a text-preserving form rather than raw date-like text; CSV cannot carry column-width or hidden-column formatting, so those requests require XLSX output instead.
- 2026-03-10: When a wildlife normalized label is renamed (for example `Rabbit` -> `Snowshoe Hare`), do not patch only the workbook rows; canonicalize the resolver output path too so old custom mappings, prompt-entered descriptions, and workbook aliases all emit the renamed label consistently.
- 2026-03-10: For the WLS `Points.xlsx` export, treat user-specified numeric columns as true numeric worksheet cells and re-check any late column-letter corrections against the current layout before preserving prior text-format workarounds.
- 2026-03-10: For generated XLSX exports, visual formatting requests like font and alignment require a real `styles.xml` part plus workbook style relationships; cell text alone is not enough.
- 2026-03-10: For the WLS `Points.xlsx` export, treat date-span columns independently: if the user adds both earliest and latest export dates, compute and write both explicitly instead of assuming the unused neighbor column should stay blank.
- 2026-03-10: In the WLS `Points.xlsx` export, exact display strings like meridian `5 -5` override numeric-cell preferences; if the user wants that literal text, keep the column as styled text and document the tradeoff instead of forcing a numeric cell.
- 2026-03-10: If the user only complains about how a normalized export field looks (for example `GS` keeping leading count numbers), prefer a targeted export sanitizer over changing broader standardization behavior unless other workflows also need the cleanup.
- 2026-03-10: When a standardized wildlife description is renamed in the workbook (for example `Snowshoe Hare Scat / Droppings` -> `Snowshoe Hare Scat`), update both the lookup rows and any runtime canonicalization aliases so old saved mappings do not keep exporting the deprecated wording.
- 2026-03-10: If a WLS export column is supposed to match the inserted table output, source both from the same standardized field (`record.WildlifeFinding`) instead of keeping a parallel cleaned-text export path that can drift.
- 2026-03-10: Broad wildlife workbook rules should not collapse species-specific phrases into generic buckets; tighten generic regexes and add an exact preserve rule when a literal phrase like `Pileated Woodpecker Feeding Cavity` must stay unchanged.
- 2026-03-10: When the user says an exact wildlife phrase should be accepted as-is, prefer a high-priority workbook preserve rule that resolves to the cleaned original text instead of weakening the broader generic category rules.
- 2026-03-10: WLS preprocess does not strip underscore-attached leading numbers like `2_` before regex matching, so preserve rules for exact phrases should allow optional leading numeric tokens when numbered variants must collapse to the same cleaned output.
- 2026-03-10: When a user approves an exact wildlife phrase like `Coyote Tracks` or `Red Squirrel Burrow`, do not assume the stock workbook already preserves it; inspect the active regex/keyword rows and add a priority-1 exact override if a broader bucket still wins.
- 2026-03-10: Exact remap requests can mix preserve-as-is and rename-to-specific-output cases in one batch; handle preserve cases with `[Keep Original]` rules, but add an explicit exact description row when the desired output is a new fixed label like `Small Rodent Sighting`.
- 2026-03-10: Leading numbered finding tags are not limited to spaced forms like `2 Foo`; shared preprocessing must also strip underscore-attached variants like `2_` and `#2_` before preserve-as-original output is built.
- 2026-03-13: When a workflow is supposed to always use a bundled resource, remove the corresponding UI setting and stale warning text too; leaving a dead path field in the palette/settings only creates confusion about what is actually configurable.
- 2026-03-13: When a workbook is the authoritative rule source and can also be edited at runtime, a backup copy is only useful if build output ships both files, load can fall back to either one, and writeback keeps both copies synchronized.
- 2026-03-13: When ATSBUILD shows zero imported dispositions/labels after a refactor, check the DWG zone/project spatial context before blaming the build runner; a missing or wrong DWG zone can make the disposition subset window miss all source geometry and surface as `Dispositions: 0`.
- 2026-03-13: For ATS shapefile import optimization sysvars, treat `MAPUSEMPOLYGON` / `POLYDISPLAY` write failures with AutoCAD `eInvalidInput` as unsupported-but-harmless on this build; log them once as skipped optimizations instead of repeating failure-style warnings that obscure real import problems.
- 2026-03-13: When correction post-processing connects L-USEC-C-0 to vertical 0/20 targets, keep seam-facing vertical extensions local; 500m-scale endpoint move caps can create obvious false spikes under correction-line townships when a distant vertical target wins.
- 2026-03-13: When a correction-township seam still looks wrong after a post-processing tweak, check the dedicated quarter-extension pass logs before changing seam cleanup again; if `SE L-USEC south 20.11 connect` reports `withH=0`, the active south-band selector is likely using the wrong offset band (10.06 instead of the normal 20.11), and later generic endpoint cleanup will only create misleading secondary spikes.
- 2026-03-13: When a user asks for Python verification before the next ATS delivery, do that before presenting the fix; use the repo DXF classifier or a geometry sanity check so the delivery is tied to the same mismatch shape the user is seeing.
- 2026-03-13: In the SE L-USEC south 20.11 bridge, allowing a boundary-gap prefilter is not enough; if the actual join can be satisfied by a bounded apparent horizontal extension (about one RA width), do not reject it with a strict on-segment distance check or the pass will stay at withH=0 and later 0/20 cleanup will create secondary spikes.
- 2026-03-13: If the latest ATS log shows the dedicated correction seam pass succeeded (for example `connected 5 SE L-USEC south 20.11...`) but the drawing is still wrong, stop tweaking the seam selector and inspect the downstream generic `0/20` cleanup passes next; they can still re-move the corrected boundaries unless those IDs are explicitly protected through the later cleanup pipeline.
- 2026-03-13: If a correction-line seam is still wrong after the dedicated seam bridge succeeds, check the late correction outer consistency and final trim stages next; a surveyed seam horizontal can survive misclassified unless the final promotion accepts `L-SEC*`, and any endpoint moves introduced after the earlier 100m trim need one last retrim before LSD rebuild or buffer spikes will persist.
- 2026-03-13: If the latest correction-line fix clearly ran but the north seam still looks unchanged, inspect the surviving source layer in quarter/log diagnostics before inventing new geometry rules; plain base `L-USEC` can slip past a late promotion that only considers surveyed/0/20/30 variants, and a post-correction retrim that still honors requested-core skip guards will not touch the visible spike lines at all.
- 2026-03-13: If a correction seam already has L-USEC-C-0 in the quarter diagnostics but the opposite boundary still resolves as ordinary L-USEC / L-USEC-0, the late correction pass must infer the missing companion band from the existing correction band at the expected 5.02m offset; only seeding from existing outer correction lines is not enough.
- 2026-03-14: If the correction-seam cleanup log shows the late correction consistency pass already ran and promoted segments, but the same seam still renders with an extra line about one standard RA width below it, stop tweaking seam-band relayers and suppress the surviving ordinary south owner bands (L-USEC-0 / L-USEC2012) under the correction seam instead.
- 2026-03-14: A correction-seam drawing can still be visibly wrong even when quarter diagnostics pick the expected correction south source; before changing quarter selection again, compare against the reference bad/good correction screenshots and the horizontal owner/fallback layer map (south: 0/20 below seam, north: 20/30 above seam) so seam-survivor cleanup targets the right ordinary layers.
- 2026-03-14: When the user calls out a correction-line issue as wrong layer classification rather than just extra survivors, add a seam-driven relayer against resolved correction seam positions before relying on cleanup-only suppression; otherwise the drawing can stay logically misclassified even if some ordinary lines are erased.

## 2026-03-14 ATS correction seam root-cause rule
- If a correction seam is on the right layer but not physically offset, do not add more relayer or survivor heuristics. Remove band-selection fallbacks and generate the correction outer/inner geometry directly from the fitted seam targets, then erase ordinary seam-band leftovers.
- 2026-03-14: When a correction seam is on the right cadence but the selected `L-USEC-C-0` still sits on the seam boundary, stop adding broader survivor/fallback relayers; choose one measured seam-axis bucket per side and normalize any `C-0` that still lands on the resolved outer axis back to `C` before quarter/LSD selection sees it.
- 2026-03-14: When a correction-line bug has already been narrowed to seam classification, do not keep iterating on the previously fixed 100m spike symptom or reinterpret `L-USEC-C-0` semantics from screenshots alone; reset to the simpler baseline and inspect whether the final correction-chain promotion simply excludes plain `L-USEC` / `L-SEC*` seam segments.
- 2026-03-14: If a user says a correction-line fix "didn't fix any of it" and the deployed `build` DLL hash matches the fresh compile, stop patching upstream inference guesses; instrument and prove which runtime classifier actually owns the bad segment before changing geometry again.
- 2026-03-14: If a correction-line fix changes the geometric seam-band math but the user still says the exact line is unchanged, do not assume the line participated in that band selector at all; inspect the targeted runtime trace for that exact segment before refining the math again.
- 2026-03-14: If the exact correction segment still stays ordinary after a perpendicular seam-band fix, inspect upstream seam candidate gates too; midpoint-only or world-axis seam-window filters can exclude long angled lines before the corrected relayer math ever runs.
- 2026-03-14: When a correction-line seam fix resolves the outer layer assignment but LSDs or the east-end 100m buffer still drift, inspect the downstream handoff passes next: `CORRZERO` endpoint overrides must choose a quarter-interior correction-zero target, and any late correction-outer promotion after the final trim needs its own retrim before LSD cleanup runs.
- 2026-03-14: If a correction-line LSD endpoint is still wrong after the seam layer fix, inspect whether the final hard-boundary pass is snapping to a boundary segment midpoint instead of the point on that segment at the LSD station; long angled or overtrimmed correction-zero horizontals can move the midpoint far enough to misplace the endpoint even when the right owner line exists.
- 2026-03-14: When late correction-line post-processing creates or promotes `L-USEC-C` / `L-USEC-C-0` geometry, include those ids in the final 100m trim too; trimming only the original context/generated sets can leave east-end correction/buffer overruns alive.
- 2026-03-14: If a correction-line LSD endpoint changes from `midpoint-kind` to `station-kind` but still lands wrong, stop tuning endpoint math and inspect whether quarter/LSD south-boundary selection is reading an outer-band survivor that was left on `L-USEC-C-0`; normalize outer-axis `C-0` segments back to `L-USEC-C` before quarter/LSD selection runs.
- 2026-03-14: If a correction-zero normalization pass runs after relayer / companion creation, do not inspect the original segment snapshot captured earlier in the method; rescan live model-space `L-USEC-C-0` entities right before normalization or the pass can silently miss the real survivors that quarter/LSD selection still sees.
- 2026-03-14: If a correction-line LSD still crosses to the south side after endpoint-target fixes, stop adjusting endpoint math and inspect the quarter south-boundary owner first; `SectionDrawingLsd` can already be preferring the far `L-USEC-C-0` correction band before any LSD endpoint enforcement runs.
- 2026-03-14: If a correction-line quarter south boundary looks right in one section but the neighboring correction sections still cross the road allowance, compare `VERIFY-QTR-SOUTH-SELECT` divider coverage next; the correction-only selector must penalize large divider gaps and prefer divider-linked candidates just like the normal south selector, or a far correction band can still win for downstream LSD endpoints.
- 2026-03-14: After selecting a correction south boundary for quarter/LSD geometry, do not let later corner overrides independently re-pick a different correction segment; reuse the chosen south segment or the corners and LSD endpoints can diverge from the verified owner.
- 2026-03-14: If correction-seam quarter logs show the south owner already on the near correction band but the LSD endpoints still hop to the far south side, inspect the later `CORRZERO/SEC` endpoint override next; it can re-rank surviving parallel correction-zero bands independently of quarter ownership, so its candidate scorer must prefer the nearest valid band rather than the smallest anchor-gap alone.
- 2026-03-14: If correction-line LSD logs still show `outer source=station-kind` with `kinds=CORRZERO/SEC`, stop patching the special `corrzero-interior` selector first; the generic `TryFindBoundaryStationTarget` preserve path may be re-running and walking an already-snapped endpoint onto a farther parallel correction-zero band on the next rule-matrix pass.
- 2026-03-14: If one correction quarter still crosses after the row-wide LSD endpoint fixes, inspect `VERIFY-QTR-SOUTH-SELECT` for that exact quarter before touching endpoint enforcement again; a lone bad quarter often means `TryResolveQuarterViewSouthCorrectionBoundaryV` accepted a non-divider-linked correction segment through its `foundAny` fallback, so reject orphaned correction candidates with large divider gaps and let that quarter fall back to the normal south owner.
- 2026-03-14: If rejecting a bad correction south owner makes one quarter go missing instead of wrong, check whether `southResolutionSegments` can still see a real ordinary fallback layer; limiting recovery to only `L-USEC-0` / `L-SEC*` can leave a quarter stuck on synthetic `fallback-20.12` even when the real surviving south edge is ordinary `L-USEC` or `L-USEC2012`.
- 2026-03-15: If a correction quarter stops crossing only after rejecting the bad full south correction owner, check whether the quarter builder still needs a local southeast correction tie before touching endpoint enforcement again; a section can legitimately lose the global south owner while the east-side quarter/LSD geometry still has to terminate on a live correction segment.
- 2026-03-15: If a correction section no longer has a valid full south owner, do not patch only one corner; rebuild the local west/east correction corners and the divider south-mid point from the same correction trend or the quarter can still cross between a corrected corner and a synthetic midpoint.
- 2026-03-15: If only the seam-transition section still fails after quarter-side fallback work, check whether correction cleanup normalized an outer-axis L-USEC-C-0 without recreating its inner companion; section-local quarter fixes cannot stay stable if the real inner correction row never exists for selection or endpoint enforcement.
- 2026-03-15: When a correction south boundary candidate behaves like the far hard boundary, do not let a partial-span segment own the whole quarter south edge. Require broad section coverage first, otherwise fall back to local correction corner construction.
- 2026-03-15: After rejecting a bad correction south owner, check every later reuse path. Quarter local-corner fallbacks and LSD endpoint overrides can quietly reintroduce the same wrong correction row even when the primary owner selection is already fixed.
- 2026-03-15: If a correction-seam LSD fix suddenly makes whole rows fall back from `L-USEC-C-0` to `L-USEC-C` / `TWENTY`, do not keep tuning ordinary fallback kinds. First check whether the correction-specific override is downgrading too early after a missed direct `CORRZERO` lookup; use an inferred inset target from live `L-USEC-C` geometry before abandoning correction ownership.
- 2026-03-15: In sec-level correction fallbacks, do not accept a raw `L-USEC-C` corner hit as an inset owner just because it is numerically closer to the inset than the hard boundary. If its south offset is implausibly small, infer the inset companion first or the quarter line can still cross the road allowance.
- 2026-03-15: When normalizing outer-axis `L-USEC-C-0` back to `L-USEC-C`, always look for an existing inner companion before creating a new one. Otherwise the normalization pass can manufacture duplicate `L-USEC-C-0` linework and send later quarter/LSD selection down the wrong branch.
- 2026-03-15: When a user says they need consistency across thousands of sections, stop adding section-local fallback/rescue branches. Move the fix upstream into deterministic owner/classification generation so quarter/LSD output comes from one consistent correction model instead of special-case recovery paths.
- 2026-03-15: If an endpoint fix starts producing targets that do not sit on real CAD entities, back it out immediately. Endpoint enforcement must only snap to actual geometry; missing correction rows have to be fixed upstream in correction-row generation, not synthesized at the endpoint stage.
- 2026-03-15: In correction companion matching, do not let a partial overlap count as an existing inner companion for a long correction row. Otherwise the generator will skip creating the real full companion and later quarter/LSD logic will fall back inconsistently.
- 2026-03-15: In correction companion matching, do not compare only absolute seam-center distance. Require the outer row and candidate companion to stay on the same signed side of the fitted seam; otherwise a north-side correction row can incorrectly pair with a south-side row across the road allowance and destabilize quarter/LSD ownership.
- 2026-03-15: If a correction section still shows `southSource=fallback-20.12` while LSD endpoints are choosing `CORRZERO`, stop tuning quarter and endpoint consumers separately. Preserve correction layer semantics end-to-end: only infer inset ownership from `L-USEC-C`, rank `CORRZERO` targets by inset-offset error before move distance, and split live horizontal `L-USEC-C` rows at vertical road-allowance boundaries so every downstream pass sees the same deterministic correction row set.
- 2026-03-15: If correction endpoints or quarter corners are landing on plain `L-USEC` geometry near a correction seam, do not keep tuning quarter/LSD selectors first. Check whether `CorrectionLinePostProcessing` is even allowed to relayer plain `L-USEC` seam-band rows into `L-USEC-C`; if the generator ignores that base layer, the true correction outer/inset pair never exists and every downstream consumer will drift or fall back differently.
- 2026-03-15: If a correction-line rerun shows no CorrectionLine: entries at all even though the cleanup pipeline says correction post-processing ran, do not assume the pass worked; check for silent zero-candidate exits and log them explicitly.
- 2026-03-15: In correction seam fitting, do not approximate a section boundary with extents midX + Top/Bottom.Y; sample the actual boundary trend from the section's left/right anchor direction and the real top/bottom boundary anchor so angled seam-transition sections stay on the same deterministic correction model as the rest of the township.
- 2026-03-15: In correction bridge cleanup, do not let a segment relayer just because one endpoint touches correction outer geometry. One-sided touch can propagate correction classification along an entire ordinary L-USEC row, creating extra west/east correction offsets and causing the final late-correction trim to erase context extensions that should stay ordinary.
## 2026-03-15 - Correction Geometry Must Normalize The Live Inset Band
- If quarter/1-4 ownership starts looking correct but LSD endpoints still downgrade off correction geometry, check whether the live inset seam row is still layered L-USEC-C even though quarter logic is treating it as L-USEC-C-0.
- Normalize live seam-overlapping inset-axis correction rows back onto L-USEC-C-0 using the fitted seam and a same-side outer companion check before trying to fix endpoint enforcement again.
## 2026-03-15 - Reuse The Quarter South Owner In LSD Endpoint Enforcement
- If quarter/1-4 geometry resolves a correction south boundary correctly but LSD endpoints still downgrade, treat that as a split source-of-truth problem instead of another seam-band heuristic issue.
- The endpoint rule matrix should reuse the same resolved quarter south correction boundary trend that quarter geometry used, especially when the live correction segment does not span the full station range but the quarter logic extends it across the section.
- When sec-6 correction cleanup is down to one extra parallel L-USEC-C row, check the final outer-consistency promotion pass before changing quarter/LSD logic; a plain span with a full L-USEC-C-0 inset companion must only be promoted when it is anchored into the correction chain at both ends.
- When LSD endpoints hit the correct correction row family but still leave a small visible gap, treat it as a model-vs-live-geometry snap issue: keep the chosen correction owner and add a final snap onto the actual live L-USEC-C-0 segment instead of changing ownership rules again.
## 2026-03-15 - Endpoint fixes must compile into the same method scope they target
- If a bug fix depends on local helpers, verify those helpers are in the same method/block scope as the call site before trusting a diff or log hypothesis.
- For ATS LSD endpoint work, prefer deterministic resolution from live collected geometry already present in the same pass instead of reaching across to helpers from a different nested pipeline.
- When correction seam data is built inside a transaction but reused later, carry the resolved seam list out explicitly instead of referencing transaction-local variables after the scope ends.
## 2026-03-15 - LSDs must stay fully deferred to the final cleanup stage
- If LSD linework has its own endpoint rules, do not draw or enforce LSD endpoints during intermediate correction/cleanup passes.
- Keep LSD generation plus endpoint snapping in one final stage after correction relayering, trimming, and outer-consistency cleanup are complete.
- When correction tie-ins introduce new vertical hard targets, add a dedicated ordinary USEC trim pass for L-USEC-0 / L-USEC-2012 instead of letting LSD cleanup compensate for ordinary road-allowance overhangs.
## 2026-03-15 - Correction tie-ins can terminate on hard correction/section verticals
- If an ordinary `L-USEC-0` / `L-USEC-2012` tie-in should stop at a correction seam or section hard boundary, do not require the trim logic to find a same-band ordinary vertical target first.
- Treat correction/section vertical hard boundaries as valid trim anchors, but still prefer same-band ordinary targets when both exist.

## 2026-03-15 - Final LSD midpoint snaps must use resolved quarter anchors
- In the final LSD rule-matrix pass, do not resolve live QSEC inner targets from the already-drifted line endpoint.
- Use the resolved quarter anchor as the reference station first, otherwise the final pass can preserve midpoint gaps instead of removing them.
## 2026-03-15 - Correction-zero outer snaps must preserve the resolved LSD station
- For correction-touching vertical LSDs, do not project the outer correction-zero target from the stale outer endpoint station.
- Use the resolved inner LSD station instead; otherwise the final endpoint can drift several meters along the same correction row even when the row owner is correct.

## 2026-03-15 - Ordinary tie-in trims should not freeze on ordinary-only contacts near correction seams
- In the final `L-USEC-0` / `L-USEC-2012` tie-in trim, an endpoint touching only ordinary same-band geometry is not strong enough to count as final ownership near a correction seam.
- Hard correction/section vertical targets should outrank ordinary targets when both are viable final stops.
## 2026-03-15 - Final tie-in trims must handle both source orientations
- Do not assume ordinary `L-USEC-0` / `L-USEC-2012` overhangs are only horizontal.
- Correction-line townships can leave the bad overhang on a vertical tie-in, so the final trim must support vertical sources to horizontal hard targets as well.

## 2026-03-15 - Verify geometric fixes with Python before declaring success
- When a user provides exact failing coordinates, reproduce the endpoint math from the current log in Python before accepting a fix.
- Do not treat a build/test pass as proof for geometry changes until the Python check matches the expected point or explains the remaining delta.

## 2026-03-15 - Ordinary USEC tie-ins need a final deterministic hard-boundary pass
- Correction-post trimming is not enough for ordinary `L-USEC-0` / `L-USEC-2012` tie-ins; the final endpoint cleanup must explicitly own them after the geometry is finalized.
- For correction-touching LSDs, any final live snap must preserve the inner quarter station, not restation from the already-shifted outer target.

## 2026-03-15 - Do not add broad ordinary USEC endpoint passes after final trim
- If a new endpoint rule can touch ordinary `L-USEC-0` / `L-USEC-2012` globally, verify in the current log exactly how many endpoints it moves before relying on it.
- Any broad endpoint pass added after the final trim must be treated as high risk for context overextensions and missing buffer linework.

## 2026-03-15 - Do not extrapolate sec-6 LSD correction targets past the resolved south-row span
- If the resolved quarter south correction row only covers one half of a section, do not project that row across the other half for LSD correction-zero targeting.
- When the LSD station lies outside the resolved row span, skip the correction-zero special path and let the generic station-ranked boundary pass own the endpoint.

## 2026-03-15 - Ordinary sec tie-ins can anchor on plain L-USEC without trimming to it
- In the final ordinary `L-USEC-0` / `L-USEC-2012` overhang trim, plain `L-USEC` rows should count as connection anchors for the opposite endpoint.
- Keep `L-USEC` out of the trim target list; it should prove connectivity, not become the final snap target.

## 2026-03-15 - Do not let sec 6 correction overrides pre-empt the generic final LSD station pass
- If the final LSD rule-matrix already has a valid `CORRZERO/SEC` station path, run that before quarter-specific correction-zero downgrade logic.
- The correction-specific target helpers are fallback/specialization paths, not the primary owner of LSD endpoints.
- When sec 6 historically had exact midpoint targets in the generic path, treat any new early downgrade as a regression until proven otherwise.

## 2026-03-15 - Do not gate live correction tie-in cleanup on seamCount alone
- Post-build cleanup may still need to trim ordinary `L-USEC-0` / `L-USEC-2012` tie-ins from live correction rows even when seam fitting itself reports zero seams.
- If a cleanup pass operates only on existing live correction layers, gate it on the presence of those live correction targets, not on whether seam discovery produced seam records.

## 2026-03-15 - Do not place live final-geometry trims behind correction seam discovery gates
- If a cleanup rule operates on already-built live correction geometry, it belongs in the unconditional final cleanup stage, not inside the correction seam builder.
- `seamCount == 0` does not mean there is no correction-owned geometry left to trim.
- When the log shows no `CorrectionLine:` lines at all, first check whether the entire owner function returned early before assuming the trim logic itself failed.

## 2026-03-15 - Prune inverse correction-band duplicates, not just promote seam-overlap USEC rows
- A remaining sec-6 overhang can be a plain USEC duplicate chain exactly one inset (`5.02 m`) away from live `L-USEC-C-0`, even when no tie-in trim is involved.
- Final correction consistency needs two directions: promote true correction chain members, and erase plain USEC duplicate survivors that shadow the live inset row.
- When one segment overlaps the inset row strongly and the next segment continues it by exact endpoint touch on the same offset side, treat that as one duplicate chain.

## 2026-03-15 - Extension bugs near correction lines can be connected ordinary chains
- If a remaining L-USEC-0 / L-USEC-2012 overhang sits about one correction inset (5.02 m) off live L-USEC-C-0, do not stop at single-row duplicate detection.
- Verify whether the bad row has an endpoint-connected continuation on the same inset side before concluding the ordinary cleanup is fixed.
- Use Python on the exact logged coordinates to prove the seed/continuation set before accepting the change.
## 2026-03-15 - Generic 0/20 endpoint cleanup can steal correction-adjoining section lines onto the wrong parallel band
- If a section-line endpoint miss differs from the expected point by about one correction inset (5.02 m), check the generic ConnectDanglingUsecZeroTwentyEndpoints pass before patching later correction cleanup.
- For correction-adjoining L-USEC-0 / L-USEC-2012 endpoints, prefer the nearby L-USEC-C-0 companion trend over the ordinary same-band target when the ordinary target sits one inset away.
- Verify this class of fix in Python using the exact wrong/expected endpoint pair and the live correction-zero row before handing it back.

## 2026-03-15 - Section-6 correction-line misses need a final ordinary 0/20 to correction-zero snap
- If sec 6 quarter logs already show southSource=L-USEC-C-0, stop patching quarter ownership and trace the later ordinary L-USEC-0 / L-USEC-2012 endpoint pass instead.
- A remaining sec 6 section-line miss that differs from the expected point by about one correction inset (~5.02 m) is a strong sign the ordinary endpoint is still sitting on the parallel non-correction row.
- Verify this class of fix with Python by projecting the current wrong endpoint along its own source line onto the live L-USEC-C-0 trend before accepting the change.
## 2026-03-15 - Verify whether a fix runs on pre-final or final correction geometry
- If a section-6 correction-line fix appears correct in code but the rerun is unchanged, check whether ApplyCorrectionLinePostBuildRules or later final-cleanup passes rebuild that geometry afterward.
- For correction-adjoining ordinary  /20 section lines, a deterministic snap belongs on the final geometry stage, not just the earlier endpoint cleanup stage.
- Add a per-run cleanup log for these late passes so a no-op cannot hide behind later correction regeneration.
## 2026-03-16 - When the owner is unclear, trace the exact surviving endpoints through the pipeline
- If repeated fixes still leave the same ordinary sec-line endpoints wrong, stop patching consumers and instrument the exact endpoints across each cleanup stage.
- Quarter-view diagnostics can confirm the south owner and still miss the real ordinary-section-line mover.
- For correction-line bugs localized to one section number, add pass-by-pass endpoint snapshots before making another behavior change.
## 2026-03-16 - When a bad endpoint only appears after a wrapper stage, trace inside the wrapper before changing logic again
- A stage snapshot like after-correction-postbuild-final can still hide which internal subpass owned the move.
- Add per-subpass snapshots and direct move logs on the exact endpoint-adjust path before attempting another geometry fix.


## 2026-03-16 - When a target endpoint can be hit by multiple correction rows in one pass, rank candidates against the original endpoint, not the current moved state
- Sec 6 failed because the same vertical ordinary line was adjusted twice inside one correction pass.
- The first snap was correct and the second farther snap overwrote it by another correction inset.
- Preserve the closest valid snap from the original seam-facing endpoint so later parallel rows cannot drag the line farther away.

- 2026-03-16: For survey LSD regressions, do not retune the L-USEC outer-kind ladder first. Verify the rule-matrix collector actually sees L-SEC polyline segments, then prefer a surveyed SEC target only when it is materially closer than the competing TWENTY/ZERO move so no-offset survey cases do not inherit correction-style L-USEC behavior.- 2026-03-16: For pure surveyed L-SEC LSD cases, do not preserve endpoints on raw SEC rows and do not treat the opposite SEC row as the target. The correct owner is the midpoint between the paired L-SEC boundaries about one RoadAllowanceSecWidthMeters apart; otherwise LSDs will cross the road allowance by a full 20.11 m instead of stopping at the center.## 2026-03-16 - For survey L-SEC LSD cases, midpoint eligibility must be decided from projected local candidates, not section-wide layer counts
- A survey road-allowance midpoint can be valid even when unrelated ZERO/TWENTY/BLIND rows exist elsewhere in the buffered section window.
- Do not gate survey midpoint logic on whole-window kind counts; gate it on whether a projected non-SEC candidate actually exists at the endpoint's station.
- Keep the survey midpoint rule restricted to `ZERO/SEC` and `TWENTY/SEC` cases so blind boundaries and L-USEC correction logic do not get retuned.
## 2026-03-16 - When a user says "midpoint of the L-SEC line," do not reinterpret it as midpoint across paired survey boundaries
- In surveyed section road-allowance cases, the correct owner can be the quarter's single-line L-SEC anchor midpoint rather than any midpoint between two parallel SEC rows.
- If the user says the line should never cross an L-SEC line, first check whether the existing quarter outer-anchor target already encodes the intended midpoint.
- Restrict survey anchor forcing to `ZERO/SEC` and `TWENTY/SEC` crossings with no projected local ZERO/TWENTY/CORRZERO owner so blind and L-USEC cases stay intact.
## 2026-03-16 - In surveyed L-SEC cases, if the endpoint already lies on the correct SEC line at the current station, do not reinterpret it as an anchor move
- A survey road-allowance LSD can already be correct before endpoint enforcement runs.
- If the latest trace shows the pre-move endpoint matches the user's expected point, the fix is to preserve or SEC-snap that station, not to add another survey-specific owner.
- For `ZERO/SEC` and `TWENTY/SEC` survey cases, prefer the live `SEC` station target over any synthetic survey anchor when no projected local ZERO/TWENTY/CORRZERO candidate exists.
## 2026-03-16 - For unsurveyed BLIND/SEC LSD cases, do not treat frequent anchor fallback as the intended owner
- If the user reports the correct landing is halfway between the `20.12` and `30.18` rows, the owner is a station-local midpoint between projected `BLIND` and `SEC` candidates, not the quarter anchor.
- Repeated `fallback-anchor moved=False` logs can still hide wrong geometry if earlier stages already pinned the endpoint to the anchor.
- In the rule-matrix, prefer a projected `BLIND/SEC` midpoint target before any anchor fallback, and keep the anchor only as the last-resort rescue path.
## 2026-03-16 - When a blind midpoint fix records `blindSecMidpointTargets=0`, stop tuning projected boundary scans and compare against the actual blind anchor
- The latest patched log showed `blindSecMidpointTargets=0` while the wrong point still matched the blind outer anchor exactly.
- For unsurveyed `BLIND/SEC` cases, if the wrong endpoint equals the blind anchor, the intended half-width owner can be the midpoint between that blind anchor and the corresponding section-side anchor on the same quarter face.
- Use the patched build log under `build/net8.0-windows` for this check; `trace-build` may still hold an older pre-fix run.
## 2026-03-16 - In unsurveyed `BLIND/SEC` LSD cases, `BLIND` is the live 30.16 band and the midpoint companion is the projected 20.11 row
- When the user reports a correct point about `5.03 m` inward from the blind endpoint, treat that as the midpoint between `L-USEC` (`30.16`) and `L-USEC2012` (`20.11`) at the same station.
- Do not midpoint `BLIND/SEC` against section anchors or generic `L-SEC` fallbacks; that creates a midpoint-of-a-midpoint overshoot.
- Guard the midpoint on a plausible `~10.05/10.06 m` blind-to-twenty companion gap before applying it.
## 2026-03-16 - For unsurveyed `BLIND/SEC` corrections, verify whether the reported offset runs along the blind boundary or across bands before changing owner logic
- The sec 30/31 miss looked like a `~5.03 m` half-band gap, but Python showed the vector ran almost entirely along the blind boundary tangent, not across the blind-to-twenty normal.
- In that shape, a same-station `BLIND -> TWENTY` search can only rediscover the current endpoint or fail; the correct owner is the midpoint of the live short blind segment the endpoint already touches.
- Before adding midpoint logic, compare the user’s expected vector against the local tangent/normal and prefer live segment-midpoint resolution over quarter-anchor fallback when the offset is tangent-aligned.
## 2026-03-16 - For vertical `BLIND/SEC` LSD outers, a tangent-aligned `~5.03 m` miss means midpoint between boundary intersections on the blind row, not midpoint of the whole blind segment
- The sec 30/31 runtime log kept showing `fallback-anchor` with `blindSecMidpointTargets=0` because the rule was searching the wrong geometry primitive.
- In these cases, the correct owner is the midpoint between the current blind-row endpoint and the first inward vertical hard-boundary intersection on that blind row.
- Do not derive this midpoint from a same-station horizontal search or from the full blind-row segment midpoint; both can preserve the wrong outer corner or overshoot badly.
## 2026-03-16 - If the LSD rule-matrix matches every line, the generic midpoint pass never runs
- `EnforceLsdLineEndpointsOnHardSectionBoundaries(...)` returns immediately after a successful rule-matrix pass, so any “simple midpoint of the final line” logic below it is skipped entirely.
- If the user’s expected result is a plain final-geometry midpoint, do not keep patching the rule-matrix ladder first; add a deterministic post-rule-matrix pass for that narrow case.
- For blind vertical LSD outers, the consistent owner is now: rule-matrix for general placement, then one final blind-boundary midpoint correction from the actual post-draw boundary segment.
## 2026-03-16 - For AutoCAD Core Console automation, trust explicit NETLOAD and plugin completion markers over raw console exit codes
- accoreconsole.exe /loadmodule did not register the managed ATS command in this environment, but scripted NETLOAD with SECURELOAD=0 did.
- Core Console can still return a non-zero process code after a successful ATS batch run because AutoCAD/Map modules surface handled exceptions during shutdown.
- For harness success, require the plugin's own ATSBUILD_XLS_BATCH exit stage: completed (ok) marker plus the exported DXF artifact; treat the raw console exit code as a warning unless those two signals are missing.- 2026-03-17: For full AutoCAD /b automation, do not trust workspace paths with spaces or incremental Compile alone. Use a no-space launcher path/junction for the scripted DWG/workbook/plugin path, and force a real Rebuild before retesting so AutoCAD does not keep loading a stale DLL while the source has moved on.
- 2026-03-17: For aligned-dimension text that should follow the final leader geometry, inspect AutoCAD's rendered anonymous dimension block at runtime; raw DXF block fields can miss or encode rotation differently, so runtime block geometry is the safer authority for correction and verification.
- 2026-03-17: If users say aligned-dimension text must stay upright, do not rotate width/disposition text to follow a sloped leader segment. Keep the text orientation to the agreed upright standard first, and solve attachment/line-follow issues with leader/dimension geometry instead of rotating the text body.

- 2026-03-17: When a post-build AutoCAD pass edits DB entities, open them ForWrite before touching geometry or text overrides. A ForRead traversal can appear harmless in .NET, but Map 3D will abort with eNotOpenForWrite once the first property setter actually mutates the entity.

- 2026-03-17: In AutoCAD post-passes, opening the target entity ForWrite is not enough if you also append new DB objects. The owning BlockTableRecord must be opened ForWrite too, or AppendEntity will abort with eNotOpenForWrite.
- 2026-03-17: When a user says aligned dimension text is "leaving the linework," don't optimize only center-to-line distance. Split continuation lines should be judged against the text-box edges and visual symmetry, or a change can look worse even when the centerpoint metric improves.
- 2026-03-17: If an aligned-dimension attachment experiment drops the aligned-dimension count or spikes `Overlap forced`, roll back the placement heuristic and move the fix to rendered follow-line geometry instead of forcing the candidate search harder.
- 2026-03-17: If the user says an aligned dimension must keep the original style, do not treat custom helper linework as a harmless fix. Prefer built-in dimension behavior and placement changes that preserve the native AutoCAD dimension presentation.
- 2026-03-18: If AutoCAD ignores per-dimension text-movement overrides and keeps generating midpoint leader artifacts, stop fighting the native dimension text engine. Use the aligned dimension for arrows/measurement only and render the upright width label as separate text.
- 2026-03-18: When the user describes aligned dimensions as a single object whose hinge cluster must stay attached to the measured line, do not split the text into separate MText. Keep the dimension single-entity and let the dimension-line offset follow the chosen text offset, enforcing only a minimum outside clearance.
- 2026-03-18: When the user provides a reference DXF for aligned dimensions, treat it as the geometry contract before trying more heuristics. Match the native DIMENSION structure first: one top-level DIMENSION entity with block-owned text/linework, not detached top-level text objects.
- 2026-03-18: When the user says moved aligned-dimension text must not drag the arrow/hinge cluster off the measured line, check the native dimension text-movement mode before adding more geometry heuristics. Keep the arrows on the measured span and let the text move with a native leader/jog instead.
- 2026-03-18: For aligned dimensions that still look detached after native placement fixes, inspect the rendered anonymous DIMENSION block and measure its actual LINE segments. Overlong rendered jog lines are a better trigger for correction than abstract text-offset guesses.
- 2026-03-18: If the user says aligned-dimension text should move outward to avoid linework instead of sliding along the measured corridor, treat along-span drift as the fallback of last resort. Solve side selection and normal-offset lanes first, then allow only small along-span movement if a side is otherwise impossible.
- 2026-03-18: If a bounded width-station scoring experiment only fixes the worst cross-section jumps by collapsing back to the old baseline, revert it instead of keeping another mixed heuristic. For aligned dimensions, keep the last verified native-DIMENSION baseline unless a new station-selection change clearly improves both overlap pressure and measured-line attachment in the AutoCAD harness.
- 2026-03-18: For native aligned-dimension attachment problems, try scoring-only penalties on long midpoint-to-text jumps before clamping candidate geometry or retuning measurement stations. Mild score changes preserved 24 aligned dimensions and improved attachment metrics, while clamping/station-hopping experiments either collapsed coverage or drifted to odd cross-sections.
- 2026-03-18: When aligned-dimension text still drifts far from the measured hinge after native candidate tuning, prefer tightening the rendered anonymous-block jogs over destabilizing the candidate search. A stronger post-pass that watches both rendered line length and raw textAlong distance preserved the 24 aligned dimensions / 9 forced overlaps baseline and improved attachment more reliably than outward-first search clamps or reduced along-step candidate sets.
- 2026-03-18: For FullAutoCAD harness runs, a cleanup failure on C:\AtsHarness\...\input.dwg can happen even after ATSBUILD_XLS_BATCH exit stage: completed (ok). Treat the run as valid if the DXF and plugin log exist, then clean up the lingering acad.exe separately instead of discarding the artifacts.

- 2026-03-18: In the WLS findings lookup, regex rules run before keyword rules and stop at the first matching priority tier; if an exact phrase like Squirrel Tracks or Beaver Den is falling through to a generic fallback, add a higher-priority specific regex (and any missing species/finding-type pair) instead of only adding a keyword row.
- 2026-03-18: After any correction-line fix, rerun quarter-definition drawing checks on correction lines separately from LSD/table ATS checks. Recent correction-line changes can leave LSD placement looking right while breaking 1/4-definition geometry or usage on those correction lines.
- 2026-03-18: For WLS lookup aliases, remember that normalization keeps plural animal words like `hares` intact. If a species regex only matches singular `hare`, pluralized labels such as `Hares_Tracks` can bypass the intended bucket and fall into generic track fallbacks unless a plural-aware regex or exact alias row is added.
