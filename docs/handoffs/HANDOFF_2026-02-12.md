# Handoff - 2026-02-12

## Current state
- Build is green and deployed.
- Latest ATSBUILD run still shows rule breaks around township edge geometry.

Latest run in `build/net8.0-windows/AtsBackgroundBuilder.log`:
- Run header: `---- ATSBUILD 2026-02-12 12:45:45 AM ----`
- Key lines:
  - `Cleanup: stitched 1 trimmed context segment endpoint(s) to nearby final boundaries.`
  - `Cleanup: seam-healed 22 L-SEC/L-USEC segment(s) near buffered boundary endpoints.`
  - `Cleanup: corner-gap-closed 18 orthogonal endpoint gap(s) on L-SEC/L-USEC.`
  - `Cleanup: simple west-end L-SEC->next L-USEC rule ... secAdjusted=7, lsdAdjusted=2.`
  - `Cleanup: enforced 20.12 stop rules ... touched30Only=19 ... unresolved=19.`
  - `RULE-WARN: 19 endpoint(s) touched 30.18 boundary but no valid inward 20.12 target was found.`

## Current error signature
- Remaining failures are concentrated in `L-SECTION-LSD` endpoints that still terminate on 30.18 corridors.
- Logged unresolved samples include:
  - Horizontal set near Y `~59718xx` with slight skew (`inward=(1.0000, ~0.006)`).
  - Vertical set near Y `~59746xx` with slight skew (`inward=(~-0.012, 0.9999)`).
- This pattern indicates township-edge trimmed/short companion segments where strict segment-hit targeting still misses valid 20.12 axes.

## What was just changed
- Added strict same-layer gating in context snap/stitch/seam passes to reduce cross-layer endpoint jumps.
- Added axis-based fallback in final 20.12 stop pass for trimmed edge geometry.
- Added unresolved sample logging (`RULE-WARN samples`) with endpoint coordinates and direction.

## Next solution to implement
1. Build a section-local orientation basis for unresolved endpoints using nearest same-layer road pair (30.18 + 20.12 companion).
2. Project unresolved LSD endpoint onto inferred 20.12 axis in that local basis (not global X/Y).
3. Clamp move to apparent intersection with local 20.12 segment extents when available; otherwise use axis hit with bounded major-gap.
4. Add "hard stop" post-pass: any LSD endpoint still touching 30.18 and not touching 20.12 gets a final deterministic correction or explicit section-tagged failure log.
5. Add per-section unresolved counters (section key + quarter) to target bad areas quickly.

## Guardrails
- Do not rotate or re-bear road allowance lines.
- Do not move non-LSD/QSEC linework in final enforcement pass.
- Preserve golden rules in `docs/specs/road-allowance/ROAD_ALLOWANCE_GOLDEN_RULES.md`.
